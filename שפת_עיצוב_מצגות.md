# שפת עיצוב — מצגות הקורס

**קובץ אחד לכל המצגות בקורס.** כל פרומפט מצגת מפנה לכאן במקום לתאר עיצוב
מחדש, כי שני תיאורים מילוליים — גם זהים — מייצרים שני קבצי CSS שונים.

**סדר קדימות:**

1. אם קיימת מצגת קודמת — **ה-CSS שלה גובר על כל מה שכתוב כאן.** מדביקים אותו
   כמו שהוא ומשתמשים באותן מחלקות בדיוק.
2. רק אם אין מצגת קודמת — בונים מהמפרט שלמטה.

הסיבה: המסמך הזה מתאר את הכוונה, אבל המצגת הקיימת **היא** התקן. אם יש פער
ביניהם — הקיימת צודקת, והמסמך הזה צריך עדכון.

---

## הרעיון

**נייר של כתב עת רפואי.** בהיר, נקי, הרבה אוויר. צבע רק היכן שהוא נושא מידע.

זו בדיוק הפלטה של מערכת ה-AI Mentor שהתלמידים נכנסים אליה. המצגות והמערכת
צריכות להיראות כמו אותו מוצר — זה מה שממתג את הקורס.

## טוקנים

```css
:root{
  --bg:#F6F9FB;        /* רקע השקף — אפור-תכלת, לא לבן טהור */
  --surface:#FFFFFF;   /* כרטיסים */
  --line:#E0E9F0;      /* קווי שיער */
  --ink:#0D1F2B;       /* טקסט ראשי */
  --ink-2:#4A6478;     /* טקסט משני */
  --ink-3:#8AA2B3;     /* תוויות מונו */
  --brand:#0891B2;
  --accent:#14B8A6;    /* צבע ההדגשה היחיד */
  --warn:#B45309;
  --warn-soft:#FFF8EC;
}
```

**אין צבעים נוספים.** אין ורוד, אין סגול, אין ירוק "הצלחה". אקסנט אחד.

## טיפוגרפיה

`Heebo` לעברית · `JetBrains Mono` לתוויות מיקרו באנגלית.

הסולם מבוסס על שקף 1920×1080. השתמשו ב-`clamp()` כדי שיישמר יחסית במסכים אחרים.

| תפקיד | גודל | משקל | צבע | הערות |
|---|---|---|---|---|
| `.display` | 112px | 700 | `--ink` | שער בלבד, `letter-spacing:-.02em` |
| `.h1` | 64px | 700 | `--ink` | כותרת שקף |
| `.h2` | 40px | 600 | `--ink` | משפט המסר |
| `.lead` | 28px | 400 | `--ink-2` | משפט משנה |
| `.body` | 20px | 400 | `--ink-2` | |
| `.stat` | 96px | 700 | `--accent` | מספר בודד. 160px בשקף שכולו מספר |
| `.mono` | 11px | 500 | `--ink-3` | uppercase, `letter-spacing:.14em` |

**`.mono` הוא מה שהופך את זה לרפואי.** בלעדיו זו מצגת כהה-בהירה גנרית.
תווית מונו אחת לפחות בכל שקף.

## מרווחים

בסיס 8px. `padding` של שקף: **72px**. בין כותרת לתוכן: **32px**.
בין שורות טקסט עצמאיות: **24px** לפחות — הרווח הוא חלק מהמסר.

## רכיבים

```css
/* כרטיס — התמונה תמיד יושבת בתוכו. אין box-shadow בשום מקום. */
.card{background:var(--surface);border:1px solid var(--line);
      border-radius:10px;overflow:hidden}

/* רצועת מלכודת — תחתית השקף, לרוחב מלא */
.pitfall{background:var(--warn-soft);border-top:2px solid var(--warn);
         height:56px;display:flex;align-items:center;gap:16px;padding:0 72px}

/* chrome של צילום מסך */
.shot-bar{height:28px;border-bottom:1px solid var(--line);
          display:flex;align-items:center;gap:6px;padding:0 12px}
.shot-bar i{width:8px;height:8px;border-radius:50%;background:var(--line)}

/* callout — עיגול פתוח + קו מוביל + תווית. אחד בלבד לשקף. */
.callout{border:2px solid var(--accent);border-radius:999px;background:none}
.leader{stroke:var(--ink-3);stroke-width:1;fill:none}

/* רשת רקע — נייר מילימטרי, לא מסך */
.grid-bg{background-image:linear-gradient(var(--line) 1px,transparent 1px),
         linear-gradient(90deg,var(--line) 1px,transparent 1px);
         background-size:40px 40px;opacity:.35}
```

## פריסות — שש, ולא ממציאים שביעית

| | הפריסה | מתי |
|---|---|---|
| `L1` | full-bleed, טקסט על אזור ריק בתמונה | שער בלבד |
| `L2` | תמונה 2/3 + טקסט 1/3 | ברירת מחדל לשקף עם תמונה |
| `L3` | רצועת תמונה עליונה ברוחב מלא + טקסט מתחת | כשהתמונה רחבה מטבעה |
| `L4` | כרטיס ממורכז, כותרת מעליו וכיתוב מתחתיו | צילומי מסך |
| `L5` | שני כרטיסים זה לצד זה | השוואות |
| `L6` | טקסט בלבד, ממורכז | שקף מסר |

## חוקי ברזל

1. **מקסימום 12 מילים גלויות לשקף.** לא נכנס — פצל.
2. **אין תבליטים.** אף פעם.
3. **התמונה תופסת 55% מהשקף לפחות.**
4. **אין טקסט על גבי תמונה.** בעיצוב בהיר זה נבלע. התמונה בכרטיס, הטקסט לידה.
   יוצא דופן יחיד: `L1`.
5. **אין צללים.** הפרדה בקו שיער 1px בלבד. רדיוס 10px מקסימום.
6. **מספר אחד גדול לשקף.** לא שניים, אלא אם השקף הוא בדיוק על הפער ביניהם.
7. **אין אימוג׳י, אין clip art, אין stock של אנשים מחייכים.**
8. **callout אחד לצילום מסך.** שקף עם שלושה חצים לא נקרא מהשורה האחורית.

## מבנה הפלט

קובץ HTML יחיד ועצמאי. שקף = `100vw × 100vh`. מעבר בחיצים ובגלילה.
CSS מוטמע. מונה שקפים בפינה ב-`.mono`. `@media print` — שקף לעמוד.

תמונות: `img/slide-NN.png` (מיוצרות) · `img/shot-NN.png` (צילומי מסך).
כל `<img>` עם `alt` תיאורי ו-placeholder לבן עם גבול מקווקו 1px ותווית מונו
של שם הקובץ — כדי שאפשר יהיה להציג לפני שהחומר מוכן.
